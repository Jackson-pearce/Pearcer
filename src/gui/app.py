"""GUI application scaffold for pearcer.

Eventually this module will provide a full desktop UI built on a
cross-platform toolkit like Qt (PySide6/PyQt6), with a multi-pane view
(packet list, details, hex view, statistics), coloring rules, and
customizable widgets (e.g., weather, calculator logos/widgets).
"""


def run_gui() -> None:  # pragma: no cover - placeholder
    """Launch the pearcer GUI.

    Currently this is just a stub. The real implementation will
    initialize the GUI framework, set up models/views, and connect to
    the capture backends.
    """

    print("pearcer GUI is not implemented yet.")
